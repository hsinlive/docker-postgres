{
  "name": "docker-postgres",
  "version": "1.0.0",
  "author": "hsinlive@gmail.com",
  "license": "MIT",
  "scripts": {
    "start": "docker-compose up -d",
    "stop": "docker-compose down",
    "psql": "docker exec -it postgres psql -U postgres",
    "bash": "docker exec -it postgres bash"
  }
}
